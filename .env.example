PORT=4000
CORS_ORIGIN=http://localhost:5173
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hftraders

# Admin panel login (/admin/login)
ADMIN_NAME=Usman
ADMIN_EMAIL=hanifusman695@gmail.com
ADMIN_PASSWORD=usman1122

# Email — leave blank to skip sending real emails; the backend will just
# log what would have been sent to the console instead.
#
# IMPORTANT (Gmail): SMTP_PASS must be a 16-character "App Password", NOT
# your normal Gmail account password. Gmail will reject your regular
# password for SMTP — it only accepts App Passwords, and only once
# 2-Step Verification is turned on for the account. Generate one at:
# https://myaccount.google.com/apppasswords
# It looks like: abcd efgh ijkl mnop (16 letters, no symbols)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=hanifusman695@gmail.com
SMTP_PASS=REPLACE_WITH_16_CHARACTER_APP_PASSWORD
MAIL_FROM=HF Traders <hanifusman695@gmail.com>
